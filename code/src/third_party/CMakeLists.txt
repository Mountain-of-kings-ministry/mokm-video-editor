# Third-party module - external dependencies and integrations

# Placeholder for third-party library subdirectories
# e.g. add_subdirectory(openfx)
# e.g. add_subdirectory(vst3_sdk)
# e.g. add_subdirectory(frei0r)

add_library(third_party INTERFACE)
# target_link_libraries(third_party INTERFACE ...)
